# libmicrohttpd-wrapper-for-FreeBasic
A simple wrapper around libmicrohttpd so it can be used with FreeBASIC

A wrapper written in C that exposes some httpd functions.  It is intended to work with FreeBasic, but should be usable with other languges.

The bas file shows an example of usage.
